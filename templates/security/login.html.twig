{% extends 'base.html.twig' %}
{% block title %}Connexion
{% endblock %}
{% block body %}
	<h1>Connexion</h1>
	{% if error %}
		<div class="alert alert-danger">{{error.message}}</div>
	{% endif %}
	{{form_start(formView)}}
	{# affiche le point de départ du form #}
	{{form_widget(formView)}}
	{# affiche les champs du form #}
	<button type="submit" class="btn btn-success">Connexion</button>
	{{form_end(formView)}}
	{# affiche la fin du form #}
{% endblock %}
